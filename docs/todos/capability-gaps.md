#Capability Gaps

Open work per capability area. Status column reflects the current level in the
`not-started → planned → scaffolded → unit-covered → integrated → runtime-wired
→ spec-covered → production-gated` ladder.

**Reading this document.** A status here is a claim, not a proof — only rows backed
by a conformance test in `tests/conformance/` are independently verified. Two
failure modes have bitten us, so check for both when auditing:

1. **Silence is not coverage.** An unlisted endpoint may be unimplemented rather
   than complete. The Application Service and Push Gateway APIs were absent
   entirely while going unmentioned here until the 0.11.11 audit.
2. **A routed endpoint is not an implemented one.** `POST /pushers/set` returned
   200 and was covered by a `spec-covered` push-rule row, while discarding every
   pusher it was given. Grep proves absence well; it does not prove behaviour.

When adding a capability, record what is *missing* as explicitly as what works.

## Capability ledger

| Capability | Status | Gap |
| --- | --- | --- |
| Build and warning policy | `production-gated` | **0.12.1 audit: mandatory fuzz execution was already closed and this row understated it** — `.github/workflows/fuzz.yml` runs on every push and pull request (not just the weekly schedule), and `scripts/run-fuzz-targets.sh` runs under `set -eu` with `-error_exitcode=77` per target, so any crash/leak/UBSan finding fails the job; no `continue-on-error`. **Platform-specific production hardening enforcement added in 0.12.1**: `scripts/check-elf-hardening.sh` statically verifies PIE/RELRO/BIND_NOW/non-exec-stack on every shipped binary (needs no live process, so it runs identically root or non-root, containerized or not) and is wired into a new `ubuntu-hardened-listener-coverage` CI job. Signed release artifacts, detached GPG signatures, and reproducible build verification landed in 0.10.38. |
| Secure configuration | `runtime-wired` | **Not touched in 0.12.1** — still needs: replace phase-specific CI naming (`scripts/validate-phase1-config.sh`, the "Validate Phase 1 configuration gates" CI step name) with capability-gate naming, and add further production profile enforcement beyond the existing TLS/reverse_proxy check. Deferred as lower-value/higher-blast-radius than the other 0.12.1 fixes; the script's actual behaviour (reject a public listener without TLS or `reverse_proxy`) is correct, only its naming is stale. |
| Runtime listener | `runtime-wired` | **0.12.1: real listener coverage gap found and closed** — audited `tests/integration/test_server_startup_hardening_flow.cpp` (the only test that spawns the real `merovingian-server` binary) and found it proved only "stays alive, shuts down cleanly", never that it *serves* anything, and that every existing CI job WARN-skipped before reaching even that (debug builds never satisfy the compiler-hardening self-check; root containers never satisfy the privilege-drop self-check). Added a real HTTP GET over a real TCP socket against the live listener plus a post-shutdown connect-refused check to that scenario, and a new `ubuntu-hardened-listener-coverage` CI job (hardened-profile build + `sudo setcap cap_setpcap+ep` on the server binary) that actually reaches those assertions instead of skipping. Add per-endpoint rate-limit accounting and a multi-listener thread pool remain open. Per-connection slowloris enforcement landed via `connection_guard`; HTTP/1.1 keep-alive (idle window, parked-connection cap, phase-aware slowloris composition) landed on `feature/release-blockers` (0.12.1). |
| HTTP transport | `runtime-wired` | Upgrade to `llhttp` or reviewed parser boundary, add request body streaming, HTTP/2, remote-IP buckets for unauthenticated routes, and operator-tunable policy overrides. Per-connection slowloris policy landed via `connection_guard`; HTTP/1.1 keep-alive landed on `feature/release-blockers` (0.12.1). Durable rate-limit state is **not** planned: counters are in-memory by design (no per-request DB write amplification), an accepted operator sign-off — see `docs/http-transport.md` "In-memory counter trade-off". `/_merovingian/admin/*` is now served and rate-limited on the client port (0.11.9). |
| Runtime concurrency | `runtime-wired` | **One recursive mutex — `HomeserverRuntime::mutex` — is held for the whole of every client-server request and every inbound federation transaction** (`src/homeserver/AGENTS.md` "The runtime lock and blocking calls"); the server serialises its hot paths. `NetworkIoUnlock` (0.11.13) releases the lock only across outbound network calls, via each entry point's `RequestLockScope` publication. Keep-alive is closed on `feature/release-blockers` (0.12.1): `src/homeserver/http_server.cpp` now serves persistent HTTP/1.1 connections (sequential request rounds, exact body draining, idle window `server.http.keep_alive_idle_seconds`, process-wide parked-connection cap `server.http.keep_alive_max_connections`, phase-aware `connection_guard` composition). **Also 0.12.1**: two lock-safety bugs found via regression-test-first evidence and fixed — `resolve_policy_server_hook` (the `trust_safety.policy_server_url` round trip) still ran under the lock from registration, room creation, and media download/thumbnail despite the federation path being fixed in #415; and `create_room` was silently recursively double-locking the mutex when called from an already-locked handler, which meant `NetworkIoUnlock` released only the outer level and the lock stayed effectively held during the "unlocked" network call — **0.11.13's `NetworkIoUnlock` was incomplete for any call chain through a second, self-locking function**, not merely missing a call site. `join_room`/`leave_room` share the same self-locking shape and plausibly the same gap; not yet fixed or confirmed, tracked as follow-up. A new opt-in load/soak harness (`tests/integration/test_runtime_lock_soak_flow.cpp`, `build_load_tests`, manual tool — no CI wiring, matching the `build_live_tests` precedent) measured the general hot path before and after: throughput/latency for reads, sends, and federation transactions were statistically indistinguishable, which is expected since neither fix touches that path when `trust_safety` is disabled. The mutex serialisation itself is therefore still a release blocker, recorded in [`production-milestone.md`](production-milestone.md) "Release-blocking functional holes" — see that row for the full evidence and remaining work (the `join_room`/`leave_room` gap, and a `/sync` stall anomaly the harness surfaced, present in both before/after runs). No case for narrowing the general hot path further has been found. |
| Client-server API | `runtime-wired` | Sync surfaces (long polling, filters, presence, to-device, account-data, sliding sync) now wired. Static TURN credentials and OIDC discovery metadata (`auth_metadata`) are implemented. `POST /_matrix/client/v3/rooms/{roomId}/invite` with `id_server`/`medium`/`address` contacts the operator-trusted identity server's `store-invite` endpoint (via the `identity` module's `IdentityServerClient`) and persists an `m.room.third_party_invite` carrying the IS-issued token and public key, per Matrix v1.19 client-server-api § Third-party invites (the IS-issued token is the event state_key). The HS fails closed with 403 when `id_server` is not in `server.identity_server.trusted_servers`; the network call goes over the SSRF-safe `OutboundClient` + `CachedServerDiscovery` (pinned addresses, private/loopback rejected) outside `runtime.mutex`. 3PID bindings are persisted via migration `006_account_threepids.sql` (was in-memory only); the IS credentials needed for IS-delegated unbind (auth mode 2: `sid`+`client_secret`) are persisted via migration `007_account_threepids_columns.sql` (0.11.10). The `bind`/`unbind`/`requestToken` handlers now drive the remote identity server when the client supplies `id_server`+`id_access_token` and the IS is operator-trusted (IS delegation is opt-in; untrusted IS → 403; absent pair → local validation unchanged); `unbind`/`delete` fail closed (502) on transport error so an IS-side binding is never orphaned (0.11.10). A discovery test-seam (`runtime.test_forced_identity_resolution` → `IdentityServerClient` 4th ctor arg) enables a self-signed local mock IS for hermetic tests (0.11.10). `GET /_matrix/client/v3/rooms/{roomId}/context/{eventId}` is now routed (0.11.11): same joined-membership gate as `/messages`/`/event/{eventId}`, `limit` clamped to the `/messages` maximum, `filter` (`RoomEventFilter`) reuses the `sync::EventTypeFilter`/`RoomFilter` machinery, and `start`/`end` reuse the `/messages` pagination token format. `m.ignored_user_list` is now enforced server-side across `/sync`, sliding sync, `/messages`, `/context`, and push delivery — see the "Ignoring users" row below (0.11.11). `GET /_matrix/client/v3/notifications` is now routed (0.11.11) — see the Client-server table. `POST /user/{userId}/openid/request_token` is now routed, paired with the federation `GET /openid/userinfo` redeem endpoint — see the Client-server and Federation API tables. `POST /_matrix/client/v3/search` is now routed (0.11.11) — see the Client-server table. `GET /_matrix/client/v1/rooms/{roomId}/threads` is now routed (0.11.12) — see the Client-server table. **Unrouted v1.19 endpoints** (audited 0.11.11, corrected 0.11.12): SSO login. The 0.11.11 audit missed `/threads`, which was unrouted and returned 404 to clients that use it (Element X calls it on every room open); treat that audit as incomplete rather than authoritative. |
| Application service API | `runtime-wired` | **Registration, auth, and outbound transaction delivery landed in 0.12.1** (`merovingian::appservice`, `include/merovingian/appservice/`, `src/appservice/`): registration files parsed as block-style YAML (`registration_yaml.cpp`, a strict bounded subset; JSON still loads as a fallback) — the earlier JSON-only parser could not read the spec's own example or any shipped bridge's `registration.yaml` build an in-memory `AppserviceRegistry` at startup from `appservice.registration_files`; `as_token`/`hs_token` are `core::SecretBuffer`-backed and compared constant-time; duplicate `id`/`as_token` across registrations is rejected at load time (spec MUST). `as_token` bearer auth plus `?user_id=`/`?device_id=` identity-assertion masquerading is wired into the client-server dispatcher via an internal, never-on-the-wire token substitution (`appservice/masquerade_token.hpp`) so every existing identity-resolution call site works unmodified. `m.login.application_service` is implemented for both `POST /register` (passwordless, bypasses UIA per spec) and `POST /login`, and advertised on `GET /login`. Namespace exclusivity (`M_EXCLUSIVE`) is enforced for username registration and room-alias creation (`PUT /directory/room/{roomAlias}`, `POST /createRoom`). `PUT /_matrix/app/v1/transactions/{txnId}` is wired synchronously into the event pipeline (local sends, local membership transitions, federation-accepted PDUs) with a durably persisted delivery cursor (`appservice_txn_cursor` table, migration `013`) — see the Application Service API protocol table below for its real, non-trivial known gaps (one event per transaction, no background retry/backoff, no ephemeral data, no ping). **Still not-started:** the outbound `GET /_matrix/app/v1/users/{userId}` / `/rooms/{roomAlias}` query hooks are scaffolded (client methods exist, unit-tested) but not invoked from any local-miss call site — the homeserver cannot yet resolve an unknown user/alias by querying an appservice on a local miss. **The third-party lookup surface landed separately** (see the `GET /_matrix/client/v3/thirdparty/*` row below): all six `/thirdparty/*` client-server routes are wired against the outbound `GET /_matrix/app/v1/thirdparty/*` calls, so bridges can now be queried for their protocol/location/user directories. **Remaining:** overlapping **exclusive** namespaces across registrations are now boot-time findings, but detection compares pattern strings — two *differing* regexes that match overlapping identifiers are not reported, because deciding regex intersection is not something a boot-time check can do and a false conflict refusing to start a correct deployment would be worse. An appservice's `sender_localpart` user is checked against every other registration's exclusive users namespace as of 0.12.2 (the server name is now threaded through `load_registrations()`/`validate_registrations()`), which matters because that user is created at startup — the collision is guaranteed, not latent. |
| Push notifications | `runtime-wired` | **Pushers are real and message and membership delivery both work — for locally-composed AND federated events — gated behind `server.push.enabled` (default `false`).** Push *rule* CRUD is `spec-covered` (see the Client-server table). `GET /_matrix/client/v3/pushers` and `POST /_matrix/client/v3/pushers/set` persist to and read from the database (`store_pusher`/`find_pusher`/`delete_pusher`/`list_pushers_for_user`), `kind: null` deletes, and `append` implements the spec's cross-user-removal rule (0.11.11). `room_service.cpp`'s `send_event()` — the choke point for `PUT .../send` and `PUT .../state` — and every membership-mutating path (`invite`/`join`/`leave`/`kick`/`ban`/`knock`, and the 3PID invite, via `persist_membership_transition` and `dispatch_membership_push_notification`) evaluate every recipient's push rules against the event and post to their "http" pushers' gateways via `PushGatewayClient`, off the request path (a detached `std::async` task, never blocking or failing the request); a pushkey the gateway rejects is deleted (0.11.11). **Events accepted over federation notify too (0.11.11 fix)**: `ingest_pdu_event()`'s caller — the `runtime.federation.pdu_sink` callback, the single convergence point for both the direct main-process federation path and the worker-relayed path — now also calls `deliver_federation_push_notifications()` after a PDU is accepted, reusing `build_pending_push_deliveries`/`dispatch_push_deliveries` unchanged; previously only `send_event()` (locally composed events) reached that pipeline, so a message from a remote room member — the ordinary federated-room case — produced no notification at all. An invitee who is not yet a joined member is still reached, whether the invite was composed locally or accepted over federation — `build_pending_push_deliveries()` takes an `extra_recipients` span alongside the room's joined members precisely so `.m.rule.invite_for_me` can fire for them (0.11.11). Background delivery tasks are bounded: completed tasks are reaped before a new one is parked (`reap_completed_futures()`, shared with `join_room`'s make_join race), and at most `k_max_in_flight_push_deliveries` (128) may run concurrently — beyond that a notification is dropped and logged rather than spawning an unbounded thread or stalling the request (0.11.11; see `docs/threat-model.md`). **Per-recipient pusher count is now bounded too (0.11.11 fix)**: a recipient could previously register an unlimited number of distinct `(app_id, pushkey)` pushers, and every notify-worthy event processed all of them sequentially inside one background task — unbounded by the 128-task cap, which only bounds task count, not the work inside one; `k_max_pushers_per_delivery` (10) now caps how many of a recipient's pushers are actually contacted per event, logging `push.pushers.truncated` (never silently) when truncated — see `docs/threat-model.md`. `room_member_display_name()` resolves a recipient's current `m.room.member` display name in a given room (falling back to the account-wide profile only when the membership event carries no `displayname`) and still feeds `PushEvaluationContext::receiving_user_display_name` on every evaluation (0.11.11 fix), but as of the PR #479 review pass (0.11.11) it no longer backs a *default* rule — see the next point. **PR #479 review pass closed three further defects**: (1) P1 — the Push Gateway API's notify Device `data` field is defined as "the data dictionary passed in at pusher creation minus the url key", but `push_gateway_client.cpp` forwarded only `format`, and the `pushers` table only ever stored `data_url`/`data_format`, so any other custom `data` member (routing/credential fields a gateway depends on) was silently dropped both at rest and at delivery; `PersistentPusher`/the `pushers` table gained a `data_extra_json` column (`migrations/011_pushers_data_extra.sql`, schema version `11`) and `PushGatewayDevice` gained a `data_extra` field that `build_device_object()` now forwards verbatim (excluding/deduplicating `url`/`format`) — **and this is now end-to-end (closed)**: `parse_pusher_set_body()` (`client_server.cpp`) captures every `data` member besides `url`/`format` into `data_extra_json`, `POST /pushers/set` persists it, `GET /pushers` echoes it back, and `room_service.cpp`'s pusher→`PushGatewayDevice` conversion threads it into `data_extra` so a live delivery carries it; proven end to end by `test_push_delivery_flow.cpp`'s custom-`data` scenario against a real mock gateway, plus a `test_client_server.cpp` registration→`GET /pushers` round trip. (2) P2 — `contains_display_name` compared the display name using the same glob engine content-rule patterns use, so a display name of literally `*` matched every message and `?` matched any single character; fixed with a literal (non-glob) word-boundary substring comparison. (3) P2 — `.m.rule.roomnotif` and `.m.rule.contains_display_name` were being shipped as server-default rules, but CS API v1.19 §push-notifications ("Predefined Rules": "the legacy default push rules that looked for mentions in the body of the event were removed" in v1.17) does not list either rule_id among its ten current default override rules — both were pre-`m.mentions` body-text-scanning rules that produced exactly the false-positive class `.m.rule.is_user_mention`/`.m.rule.is_room_mention` (structured `m.mentions`) replaced (a message merely containing someone's name, or the literal text "@room", highlighted the recipient with no `m.mentions` involved at all); both are removed from `default_push_ruleset.cpp`, though the `contains_display_name` *condition kind* itself remains valid (deprecated, not removed) for a hand-authored rule. `GET /_matrix/client/v3/notifications` is now routed (0.11.11, see the Client-server table) — history is recorded in `build_pending_push_deliveries()` unconditionally on `push.enabled`/pushers, so it works the same whether or not the recipient has push configured, and now covers federated events too. **Known gaps**: "email" pushers are accepted and persisted but never delivered (no email transport); there is no gateway retry/backoff (spec SHOULD, not MUST); `POST /pushers/set` itself has no per-user registration limit — the cap above is delivery-side only, so an already-registered excess of pushers is bounded at delivery time but not rejected at registration time. |
| Authentication and sessions | `runtime-wired` | Server-side access/refresh token expiry with configurable lifetimes, hashed registration-token validity, and constant-time token-hash lookups landed in 0.8.20. **0.12.4: `POST /_matrix/client/v3/account/deactivate` implemented** — see the Client-server API protocol table below for what it does and does not do (it does not erase content, unbind 3PIDs, or leave rooms). **Also 0.12.4:** a per-account failed-login throttle (five failures/15 minutes locks the claimed user ID out for 15 minutes, `429`) closes the gap where `/login`'s per-user rate-limit tier — keyed on the *authenticated* user — never applied pre-login; see `docs/auth-identity.md` "Per-account failed-login throttle". **Known gap:** the throttle's thresholds (attempt count, window, lockout duration) are compile-time constants, not configuration — an operator cannot tune them without a rebuild. Add richer operator bootstrap lifecycle controls, account recovery controls, and Matrix conformance fixtures for remaining auth flows. |
| E2EE key APIs | `spec-covered` | Key backup version management, session retrieval, count, etag, and session/room/batch deletion endpoints are wired. `POST /_matrix/client/v3/keys/upload` reports one-time key counts grouped by advertised algorithm. `GET /_matrix/client/v3/keys/changes` now validates `from`/`to` sync tokens and filters results to the requested stream range, with conformance coverage for missing/invalid parameters and stream-boundary behaviour. Cross-device verification: `/_matrix/client/v3/keys/upload` and `/_matrix/client/v3/login` (on new-device creation) record a `device_lists.changed` self-notification so a user's own devices discover each other and a freshly-logged-in device fetches existing devices' keys on its first `/sync` (0.11.8). Remaining: broader Matrix v1.19 semantics and additional conformance fixtures. |
| Rooms, events, and sync | `runtime-wired` | Sync long polling, filters, presence, to-device, account-data, and restricted/restricted_v2 join rule evaluation now wired. Third-party invite auth (rule 4.3.1 signature/token validation on `m.room.member` invites, plus invite-power gating on `m.room.third_party_invite` creation) landed and is wired into the local send path, inbound federation PDU path, and state resolution. **0.12.4: authorization rules 1, 8, and 9 enforced in full** (`src/events/authorization.cpp`) — rule 9 (`m.room.power_levels` changes) previously covered only `content.users` (sub-rules 9.8/9.9); sub-rules 9.1-9.3, 9.5-9.7 were absent, so a sender who only met the room's *old* `state_default` could set `users_default` above their own level, or zero out `ban`/`kick`, and a `content.events` fallback in `extract_power_level_key` gave a second route to the same escalation by smuggling scalar values in under an event-type key. Rule 1 (`m.room.create` room-ID/domain/version validation) was unimplemented in every sub-rule. Rule 8 (an `@`-prefixed `state_key` must match the sender) was absent. Separately, the client-originated event path's authorization call was wrapped in nested guards with no `else` branch, so a parse failure or unrecognised room version composed the event with no authorization at all rather than reaching the existing rule-2 denial. See `docs/event-engine.md` and `docs/threat-model.md` for the full writeup; this row is updated because the fix changes what "runtime-wired" room event authorization actually covers. `POST /_matrix/client/v3/rooms/{roomId}/invite` with `id_server`/`medium`/`address` round-trips to the trusted identity server's `store-invite` endpoint and persists an `m.room.third_party_invite` carrying the IS-issued token/public key (no longer local-only; 0.11.9), and `POST /_matrix/client/v3/rooms/{roomId}/join` accepts `third_party_signed` objects for matching same-server invites by verifying the embedded signature and creating the intermediate invite member event. Room-version policy table is conformance-covered, and a runtime conformance scenario now creates rooms for every stable version v1–v12 and verifies the create event records the requested version. MSC4186 multi-list combination (a room in more than one list window has ALL matching list windows combined, not just the first) landed in 0.11.10. |
| Federation | `runtime-wired` | Room-version-specific PDU verification is now conformance-covered for every stable version v1-v12 (reference-hash event IDs, v12 implicit create/auth_events rules). Simultaneously-active multiple Ed25519 signing keys are now supported and conformance-covered. Remaining: broader Matrix federation conformance coverage. Key-rotation publication with `old_verify_keys` landed in 0.8.6. **Pre-authentication remote-key resolution is budgeted (0.12.4).** `resolve_inbound_remote` runs before `check_inbound_request_signature` and, for an origin it has no cached key for, calls `remote_key_resolver`, which performs `.well-known` + SRV + DNS discovery and an outbound `GET /_matrix/key/v2/server`. Verifying the signature requires the key, so the ordering is inherent and cannot be reversed. With the shipped `security.federation.default_policy=allow`, an unauthenticated sender can therefore make this server perform outbound requests to a host of their choosing, by naming any origin in an `X-Matrix` header. `CachedServerDiscovery` caches results (including failures) but is keyed on the server name the attacker controls, so varying the name misses the cache every time; failed key fetches are not negatively cached at all. This is both a DoS surface on this server and a reflection vector at the named third party. `default_policy=deny` with a populated `allowed_servers` list closes it independently -- the policy check runs before resolution -- so allow-list deployments were never exposed. Now bounded by three restart-scoped budgets keyed on the source IP rather than the origin: `key_resolution_per_ip_rate` (default 10/60s), `key_resolution_max_in_flight` (default 8, rejecting rather than queuing over the cap) and `key_resolution_failure_ttl` (default 300s negative cache). Both pre-auth containers are explicitly capped with FIFO eviction, since an unbounded pre-auth map would be the same class of bug. A legitimate peer needs one resolution before its record is cached. **Remaining:** the budgets are keyed on source IP, so a sender distributed across many addresses still consumes up to the in-flight cap; that cap is the backstop and is deliberately conservative. |
| Media repository | `spec-covered` | Live remote media transport and server discovery wired in v0.7.2. Real image resampling landed in v0.8.10 via the sandboxed out-of-process `merovingian-thumbnail-worker` (libpng/libjpeg-turbo), generated on demand per requested geometry. Outbound remote fetch tries the mandatory authenticated `GET /_matrix/federation/v1/media/download/{mediaId}` endpoint first, falling back to the deprecated `/_matrix/media/v3/download/{serverName}/{mediaId}` endpoint only on 404 (v0.10.34). The inbound side of the same endpoint — serving locally-uploaded media to other homeservers over federation with `X-Matrix` authentication and a `multipart/mixed` response — landed in v0.10.38. `Location` redirects are followed via an SSRF-safe resolver with pinned addresses (v0.11.5). The `multipart/mixed` parser was hardened to strict RFC 2046 semantics in v0.11.6: boundary delimiters must sit on their own line, quoted/unquoted boundary tokens with optional whitespace are accepted, preambles and LF-only transport padding are tolerated, and the parser fails closed unless exactly two parts are present. **0.12.4: client-facing download/thumbnail responses now carry `Content-Security-Policy`, `Cross-Origin-Resource-Policy: cross-origin`, and `Content-Disposition`** (`inline` only for the spec's "Serving inline content" allowlist, `attachment` otherwise) — see `docs/media-repository.md` "Response security headers". **Known gap:** no `filename` parameter on `Content-Disposition`, because this server does not persist the upload's original filename (spec permits omitting it, but a client cannot recover the original filename from a download response either — schema and media-pipeline change needed to close this). Remaining: Matrix v1.19 remote-thumbnail conformance fixtures. |
| Database persistence | `runtime-wired` | **0.12.1: PostgreSQL role-grant enforcement audited.** The CI/test-level proof this row originally referred to (transaction-rollback, migration-ordering, and role-grant durability tests, real temp Postgres via `postgres-integration.yml`) was already closed since 0.8.6/0.8.9 and is not a gap. What actually was missing — a packaging-side way to provision the two roles for a real deployment — is now `packaging/postgresql/provision-roles.sql` (0.12.1), the same grant pattern CI already proves against a live database. **Still open:** nothing in the live startup path calls `set_postgresql_role()` — `merovingian-db-migrate` never opens a database connection at all (it only prints an offline plan from `build_offline_migration_plan`), and `bootstrap_local_database()` always applies migrations automatically through the same connection/role that then serves runtime traffic, so today the login role in `database.uri_file` always needs DDL rights regardless of whether the two roles above have been provisioned. See `docs/database-persistence.md` "Next starting points" for the exact remaining wiring (db-migrate becoming a real live-migration tool; the runtime bootstrap `SET ROLE`-ing to the runtime role and refusing to auto-migrate under it). |
| Observability and audit | `production-gated` | Prometheus text exposition, bounded admin correlation headers, and structured request correlation landed in 0.8.11. Remaining: operator dashboards and retention/export policy for long-term audit archives. |
| Trust and safety | `runtime-wired` | Remote HTTPS policy-server transport and admin policy-rule management workflows are now wired into registration, room creation, inbound federation, media download, and admin review flows. `POST /_matrix/client/v3/rooms/{roomId}/report/{eventId}` and `GET /_matrix/client/v3/admin/safety/reports` are conformance-covered. `GET/PUT/DELETE /_matrix/client/v3/admin/safety/policy_rules/{scope}/{entity}` is now conformance-covered for CRUD, invalid actions, missing rules, and non-admin guards. Remaining: moderator queues and broader workflow coverage. |
| Runtime hardening | `runtime-wired` | ELF program-header probe (linker/RELRO) retired in v0.7.2; seccomp-bpf allowlist with `SECCOMP_RET_KILL_PROCESS` default, `RLIMIT_CORE` clamp, `no_new_privs`, and capability bounding set drop all landed in v0.8.18. OpenBSD pledge/unveil and FreeBSD Capsicum landed in v0.10.7 with CI test coverage. **0.12.1: closed a genuine fail-open gap** — `apply_worker_hardening()` (`src/platform/runtime_hardening.cpp`) unconditionally returned `accept()` on every non-Linux platform regardless of `federation.worker.apply_hardening`, so the federation worker logged "runtime hardening applied" and handled untrusted federation traffic completely unsandboxed on FreeBSD/OpenBSD/NetBSD, with no test coverage anywhere. Now fails closed (`worker_hardening_unavailable_decision()`) and refuses to start instead. `hardening_self_check.cpp`'s own `is_ready()` gate was already correctly strict (any `unknown` or `disabled` check blocks startup) — the gap was specifically in the worker's separate hardening path, not the main process's self-check. Remaining: optional in-process privilege drop, Landlock confinement, and platform-specific (pledge/unveil- or Capsicum-equivalent) sandboxing for the federation worker itself on BSD, which today has no in-process sandbox at all and simply refuses to run there under `apply_hardening=true`. |
| Platform support | `runtime-wired` | OpenBSD and NetBSD CI jobs added (full build + test suite per PR via `vmactions/*-vm`), joining Linux/Fedora/FreeBSD as Tier 1; platform runtime tests run on each via the suite; support tiers documented in [platform-support.md](../platform-support.md). pledge/unveil and Capsicum now wired and tested in CI (v0.10.7). Remaining: in-process privilege drop parity and Landlock. |
| Fuzzing and conformance | `integrated` | Five new fuzz targets added in 0.8.14 (sync filter, config parser, stream token, query params, SRV record) with checked-in seed corpus and automated seeding in CI; confirmed in 0.12.1 that `fuzz.yml` already runs this mandatorily on every push/PR (see the Build and warning policy row) — that part of this row's old "remaining" text was stale. Remaining, **not attempted in 0.12.1 — out of budget for this pass**: property tests, load tests, chaos tests, and a broader Matrix conformance suite. **Complement audited in 0.12.1**: `tests/fixtures/complement/` is this project's own lightweight JSON-fixture-driven flow test (`test_sync_complement_fixture.cpp`), not the upstream `matrix-org/complement` Go black-box test suite the production-milestone bullet means. A root `Dockerfile` exists but is not Complement-compatible (no `SERVER_NAME`-driven config, no federation TLS via Complement's provided CA, no fixed listen ports); there is no Go toolchain wiring, blueprint registration, or CI job anywhere in the repo. Standing up real upstream Complement in CI is a substantial, separate piece of work (Complement-shaped container image + a new CI job with Docker + Go) and was not started. |
| Supply chain and release | `production-gated` | Debian, Fedora, RHEL-compatible, OpenSUSE, FreeBSD, OpenBSD, and NetBSD packages and release tarballs carry SLSA provenance attestations via `actions/attest-build-provenance` (verifiable with `gh attestation verify`) and detached GPG signatures by the maintainer key. The dependency pinning policy (`[wrap-file]` with SHA-256 hashes), license review artifact (`merovingian.licenses.json`), and reproducible static-Linux-tarball verification (`scripts/reproducible-build.sh`) landed in 0.10.38. **0.12.1: release-evidence recording landed** — `scripts/collect-release-evidence.sh`, run per-platform in `release.yml`, records the actual compiler version, confirms link-time hardening via `check-elf-hardening.sh`, lists pinned dependency versions from `subprojects/*.wrap`, summarizes the test log Ok/Fail/Timeout counts, and lists the mandatory fuzz target names; `publish-alpha-release` folds every platform's evidence plus package checksums into the published release notes. |

## Protocol coverage gaps

### Client-server API

| Endpoint | Status | Needs |
| --- | --- | --- |
| `GET /_matrix/client/v1/auth_metadata`, MSC2965 OIDC | `spec-covered` | Discovery metadata implemented; full OAuth 2.0 flow remains future work. |
| `GET /_matrix/client/v3/voip/turnServer` | `spec-covered` | Static TURN credentials from `server.turn.*` configuration; shared-secret time-limited usernames are not yet implemented. |
| `GET /_matrix/media/v3/thumbnail/*`, `GET /_matrix/client/v1/media/thumbnail/*` | `spec-covered` | Real image resampling (PNG/JPEG, `scale`/`crop`) via the sandboxed worker landed in 0.8.10. Remote thumbnails fetch via the authenticated federation media endpoint and resample locally instead of serving the full-size original (v0.11.5). Conformance coverage now verifies that a remote thumbnail request never returns 200 with the full-size original bytes when federation infrastructure is unavailable (v0.11.5). |
| `GET /_matrix/client/v1/media/config` | `spec-covered` | |
| `GET /_matrix/client/v3/directory/list/room/{roomId}` | `spec-covered` | |
| `PUT /_matrix/client/v3/directory/list/room/{roomId}` | `spec-covered` | |
| `GET /_matrix/client/v3/rooms/{roomId}/aliases` | `spec-covered` | |
| `GET /_matrix/client/v3/rooms/{roomId}/joined_members` | `spec-covered` | |
| `GET /_matrix/client/v3/rooms/{roomId}/event/{eventId}` | `spec-covered` | |
| `POST /_matrix/client/v3/rooms/{roomId}/upgrade` | `spec-covered` | Full state copy (name, topic, join rules, power levels, etc.) to replacement room. |
| `POST /_matrix/client/v3/rooms/{roomId}/receipt/{receiptType}/{eventId}` | `spec-covered` | |
| `POST /_matrix/client/v3/user_directory/search` | `spec-covered` | |
| `POST /_matrix/client/v3/delete_devices` | `spec-covered` | |
| `GET /_matrix/client/v3/sync` | `spec-covered` | Unread-notification counts are relative to the user's last read receipt and bound to the response snapshot. The `summary` object (`m.joined_member_count`, `m.invited_member_count`, `m.heroes`) is now emitted for every joined room and covered by conformance tests. `filter_id` parameter now wired and conformance-covered; sync-only stream tokens now bound to the response snapshot for to-device delivery so room keys queued during response construction remain pending for the next sync. |
| `GET /_matrix/client/v3/keys/changes` | `spec-covered` | Requires and validates `from` and `to` sync tokens, decodes them to `sync_stream_id` boundaries, and filters `changed`/`left` device-list changes to that half-open range. Conformance coverage includes missing/invalid parameters and an upper-bound test proving `to` excludes later changes. |
| `POST /_matrix/client/v4/sync` and `POST /_matrix/client/unstable/org.matrix.msc4186/sync` (MSC4186 Simplified Sliding Sync) | `spec-covered` | Advertised via `unstable_features["org.matrix.msc4186"] = true` and the compatibility alias `unstable_features["org.matrix.simplified_msc3575"] = true`; the stable `v4` endpoint is now also routed and accepts the same request shape. Implements named lists with windowed `ranges` or singular `range`, SYNC/INVALIDATE/INSERT/DELETE/UPDATE ops, per-room state/timeline, room subscriptions, and all five extensions (to_device, e2ee, account_data, receipts, typing). Long-poll via sync pool; `pos` and `timeout` may be supplied in query parameters or in the JSON body. List + `room_subscriptions` merge: when a room is in both a list window and a `room_subscriptions` entry, `required_state` is the superset (dedup + wildcard-aware), `timeline_limit` is the max, and `include_heroes` is OR'd (previously the subscription overrode the list wholesale) — landed 0.11.9. Multi-list combination: a room in MORE THAN ONE list window now has ALL matching list windows combined (left-fold over `combine_room_configs`, associative+commutative) with each other and the `room_subscriptions` entry, not just the first matching list — landed 0.11.10. Remaining: broader MSC4186 conformance fixtures and subscription timeline pruning. |
| `POST /_matrix/client/v3/account/password` | `spec-covered` | UI-auth re-authentication. `logout_devices` revocation (other devices' access/refresh tokens and sessions) landed in 0.8.19. |
| `POST /_matrix/client/v3/account/deactivate` | `spec-covered` | Implemented 0.12.4 (previously unhandled — see `docs/auth-identity.md` "Account deactivation"). UIA (`m.login.password`), permanent deactivation (schema version 14 `users.deactivated` column), full access/refresh-token revocation including the caller's own session, password hash overwritten with an unmatchable value, and the `users` row retained so the localpart is never reissued. Every identity-server-bound 3PID the account holds is unbound as part of deactivation, reusing the stored `id_server` + `client_secret` + `sid` (migration `007`) to drive IS unbind auth mode 2 through the same trusted-base-URL SSRF gate as `POST /account/3pid/unbind`. `id_server_unbind_result` is `success` only when every identifier was unbound (and when there were none), `no-support` when any could not be — no identity server recorded, operator trust withdrawn for it, or the IS unreachable or refusing. Unlike the standalone unbind endpoint, which fails closed so the user can retry, a failure here still deactivates and still clears the local binding: deactivation is irreversible and there is no owner left to retry. **Known gaps:** the `erase` request-body parameter is parsed but never inspected, so it has no effect regardless of its value (erasure is a spec SHOULD, not a MUST, so this is not itself non-conformant, but a client requesting erasure is silently no-op'd); and the account's rooms are not left. |
| `POST /_matrix/client/v3/createRoom` | `spec-covered` | Broader conformance fixtures. |
| `POST /_matrix/client/v3/rooms/{roomId}/join` | `spec-covered` | Federation-aware joins. |
| `POST /_matrix/client/v3/join/{roomIdOrAlias}` | `spec-covered` | Room-alias resolution, `?server_name` hint, federation-aware joins. |
| `POST /_matrix/client/v3/rooms/{roomId}/send` | `spec-covered` | Restricted and restricted_v2 join rule evaluation, and third-party invite auth (rule 4.3.1), landed in `events/authorization.cpp`. |
| `PUT /_matrix/client/v3/user/{userId}/account_data/{type}` | `spec-covered` | Room-scoped account data (`/rooms/{roomId}/account_data/{type}`). |
| `GET/PUT/DELETE /_matrix/client/v3/user/{userId}/rooms/{roomId}/tags[/{tag}]` | `spec-covered` | Room tagging via `m.tag` account data; `order` doubles supported in general JSON parser. |
| Push rule CRUD | `spec-covered` | Writable push-rule CRUD (PUT/DELETE/enabled/actions). Rules are stored and served correctly, but nothing evaluates them against the event stream — see `GET /pushers` below and the Push notifications capability row. |
| `GET /_matrix/client/v3/pushers` | `spec-covered` | Returns the caller's persisted pushers in the spec's `Pusher` shape (0.11.11). Conformance-covered (round-trip with `POST /pushers/set`). |
| `POST /_matrix/client/v3/pushers/set` | `spec-covered` | Persists via `store_pusher`; `kind: null` deletes via `delete_pusher`; `append` implements the spec's cross-user pusher-removal rule (0.11.11). Body validation (including the `https` + `/_matrix/push/v1/notify` pusher-URL check) unchanged. Conformance-covered. |
| `GET /_matrix/client/v3/notifications` | `spec-covered` | Routed (0.11.11): paginates via `from`/`next_token` (the plain stream-ordering token format `/messages` already uses — `next_token` names the first not-yet-returned notification's `stream_ordering`); `only=highlight` filters to highlight-tweaked notifications; `limit` defaults to 50, clamped to 1000; `read` reflects the caller's own `m.read`/`m.read.private` receipt in that room, computed at request time via `sync::read_receipt_ordering()` rather than stored. History is recorded in `room_service.cpp`'s `build_pending_push_deliveries()` for every local recipient whose push rule evaluation resolves `notify: true` — unconditionally on `server.push.enabled` and on the recipient having a pusher, so a user with push turned off still sees their notifications; only actual gateway delivery stays gated on those. Persisted via the new `notifications` table (`migrations/009_notifications.sql`, schema version 9), pruned per-user beyond `k_max_notifications_per_user` (200) at write time so the table cannot grow without bound. `profile_tag` is always empty: recording happens once per `(user, event)` rather than once per pusher, so it cannot name one pusher's configured tag. |
| `GET /_matrix/client/v1/rooms/{roomId}/threads` | `spec-covered` | Routed (0.11.12): lists the room's thread roots ordered by each thread's `latest_event`, most recently active first, with the bundled `m.thread` aggregation (`latest_event`, `count`, `current_user_participated`) on every returned root. `include=all|participated` is honoured and any other value is rejected with 400 `M_INVALID_PARAM` rather than silently falling back to `all`; `limit` defaults to 50 and is capped at 500; `from`/`next_batch` paginate on the latest_event stream ordering, and `next_batch` is omitted when the page is the last one. `m.ignored_user_list` is applied via `trust_safety::ignore_list`, resolved once per request: child events from ignored senders are excluded from `count`/`latest_event` (spec MUST), and a root sent by an ignored user is returned redacted rather than omitted. Joined-membership gate, 403 for an unknown room or a room the caller cannot view. **Known gaps**: bundled `m.thread` aggregations are produced only by this endpoint — `/sync`, `/messages`, `/context`, `/relations` and `/event/{eventId}` still return events without an `m.relations` block, so a client relying on aggregations elsewhere must aggregate locally; threads in rooms the caller has left are not listed (the same join-only gate the other read endpoints use); and ordering is by the server's stream ordering rather than a full topological ordering. Like `/relations`, the handler scans the in-memory `PersistentStore::events` for the room on every request — `limit` bounds the response, not the scan — so cost grows with room history rather than with page size. |
| `POST /_matrix/client/v3/search` | `spec-covered` | Routed (0.11.11): in-memory search over `PersistentStore::events` (no SQL full-text index — see `docs/architecture.md`), scoped to rooms the caller is currently *joined* to (not the spec's fuller "including rooms you have left" scope — the same join-only membership gate `/messages`/`/context`/`/event/{eventId}` already use), and never searching end-to-end encrypted rooms (spec MUST). Searches `content.body` (m.room.message), `content.name` (m.room.name), and `content.topic` (m.room.topic) via a case-insensitive substring match — not stemmed/tokenised full text search. `filter` (`RoomEventFilter`, reusing `sync::parse_room_event_filter_argument`), `keys`, `order_by` (`rank`/`recent`), `event_context` (`before_limit`/`after_limit`/`include_profile`), `include_state`, and `groupings` (`room_id`/`sender`) are all honoured. `m.ignored_user_list` is applied via `trust_safety::ignore_list`, resolved once per request. Bounded: `ClientApiLimits::max_search_events_scanned` (2000) caps how many candidate events one request will JSON-parse and text-match before returning a `next_batch` continuation instead of scanning further; page size is separately capped at 25; rate-limited at 20/min per IP (the media tier) rather than the generic 90/min fallback. **Known gaps**: rooms the caller has left are never searched (join-only, see above); `order_by: rank` is only locally accurate within one request's bounded scan window, not a global top-K across the caller's entire matching history — a client paginating in rank order does not get a globally rank-sorted stream; per-group `next_batch` (pagination *within* one group) is not implemented, only the top-level `next_batch` (spec explicitly allows a server not to support this); `profile_info` returns each sender's *current* profile, not a point-in-time historic snapshot; and the MSC3765 extensible-topic `content['m.topic']` text/plain representation referenced in the spec's room_events section is not indexed (only legacy `content.topic`). Conformance-covered in `tests/conformance/test_search_conformance.cpp`, including the access-control property that an event in a room the caller is not joined to, or from an ignored sender, or in an encrypted room, is never returned. |
| `GET /_matrix/client/v3/rooms/{roomId}/context/{eventId}` | `spec-covered` | Returns `events_before`/`event`/`events_after`/`state` around a permalink target. Same joined-membership gate as `/messages` and `/event/{eventId}` (non-members 403; unknown event id and an event id from a different room both 404 `M_NOT_FOUND`, identically, so existence elsewhere is not leaked). `limit` (default 10, spec-applied to the combined `events_before`+`events_after` size) is clamped to the `/messages` maximum. `filter` accepts a `RoomEventFilter` via `sync::parse_room_event_filter_argument()`; malformed JSON is rejected with 400 `M_BAD_JSON`. `start`/`end` are the same plain stream-ordering tokens `/messages` produces. **`state` is now reconstructed at the last event actually returned (0.11.11, PR #479 review fix)** rather than read off the room's current state, per CS API: "The state of the room at the last event returned." `federation::resolve_state_event_ids_at()` (`src/federation/event_query.cpp`) reuses the same backward event-DAG walk `build_state_response`/`build_state_ids_response` already use for the federation `GET /state`/`/state_ids` endpoints (`reconstruct_state_at_event`, landed 0.8.10), then folds the pinned event's own state contribution back in when it is itself a state event — the federation endpoints intentionally stop one step short of that (SS API `GET /state`: "prior to considering any state changes induced by the requested event"), while `/context`'s `state` wants the room's state inclusive of the pinned event. Proven by a conformance scenario where a room's name changes strictly after the last event a bounded `limit` admits: `state` still carries the pre-rename name, not the room's present-day one. **`GET /messages` was deliberately left unchanged** — see its own row below for why extending this fix there does not apply cleanly. |
| `GET /_matrix/client/v3/rooms/{roomId}/messages` | `spec-covered` | Paginates the room timeline via `from`/`to`/`dir`/`limit`/`filter` (`RoomEventFilter`). **Chunk-relevant `state` landed in 0.12.2**, closing the divergence from `/context` recorded here: `lazy_load_members` was never parsed anywhere in the codebase, and this endpoint passed a default-constructed filter to its state builder, so it returned the room's full current state including every member. Membership state is now limited to senders appearing in the chunk. The endpoint-shape distinction is worth keeping: `/context` returns "the state of the room at the last event returned" and reconstructs it via the event-DAG walk, whereas `/messages` returns "state events relevant to showing the `chunk`" — so applying `/context`'s temporal fix here would have left `state` wrong in a different way (over-inclusive, unfiltered by relevance) rather than right. |
| `POST /_matrix/client/v3/user/{userId}/openid/request_token` | `spec-covered` | Mints a short-lived OpenID token (`request_openid_token`), stored in its own `openid_tokens` table (migration `010_openid_tokens.sql`, schema version 10) — never the `access_tokens` table — so it cannot authenticate ordinary client-server requests. Path `userId` must equal the authenticated caller; a mismatch fails closed 403 `M_FORBIDDEN` identically whether or not the named user exists. Redeemed via the paired federation `GET /_matrix/federation/v1/openid/userinfo` (see the Federation API table); conformance-covered for both halves plus the access-token/OpenID-token separation in both directions. |
| SSO login (`m.login.sso`, `GET /_matrix/client/v3/login/sso/redirect[/{idpId}]`) | `runtime-wired` | `m.login.sso` is advertised from `GET /login` (with `identity_providers`) when `server.sso.*` is configured; `GET /login/sso/redirect[/{idpId}]` is routed, validates `redirectUrl` against an operator allowlist (`server.sso.redirect_url_allowlist`) to prevent an open redirect, validates `idpId` against `identity_providers` (404 for unrecognised), and 302s to the operator-configured `server.sso.authorization_url`. `m.login.token` is now implemented in `POST /login` (previously entirely absent, despite this row's earlier claim) — `homeserver::complete_sso_login` mints a short-lived, single-use login token (durable `login_tokens` table, migration `012_login_tokens.sql`, disjoint from `access_tokens` for the same token-confusion reasons `openid_tokens` is) and `homeserver::redeem_login_token` consumes it exactly once. **Remaining:** Merovingian does not itself speak an external SSO protocol (CAS/SAML/OIDC) — `complete_sso_login` is the integration point an operator's own external-IdP adapter calls after authenticating the user and mapping them to a local Matrix user id; a first-party adapter for a specific protocol is not implemented (0.12.1). See `docs/auth-identity.md` ("SSO login") and `docs/threat-model.md` ("Open redirect and login-token exfiltration via SSO redirectUrl"). |
| Ignoring users (`m.ignored_user_list`) | `spec-covered` | Enforced (0.11.11) via `merovingian::trust_safety::ignore_list` (`resolve_ignored_users` / `is_delivery_suppressed`), called from every client-facing delivery surface: `GET /sync` (timeline, invite list, typing/receipts), MSC4186 sliding sync (timeline, required_state — including the invite override for an explicitly-subscribed room, since sliding sync has no separate invite surface — and the receipts/typing extensions), `GET /messages`, `GET /context/{eventId}` (events_before/events_after only — the requested event itself is always returned), and push notification delivery (`build_pending_push_deliveries`, covering both message and membership/invite pushes). Per spec: non-state events from an ignored sender are withheld; state events are still delivered (room state stays accurate); a new-room invite is withheld even though it is itself a state event. Filtering is delivery-side only — it never touches event persistence, auth rules, state resolution, or federation acceptance, and it does not retroactively hide events already delivered to the client (a fresh sync is required for that, per spec). A malformed or absent `m.ignored_user_list` fails safe to "nothing ignored". Conformance-covered in `tests/conformance/test_ignoring_users_conformance.cpp`; sliding sync and push-delivery suppression are integration-covered in `tests/integration/test_sliding_sync_flow.cpp` and `tests/integration/test_push_delivery_flow.cpp`. **Known gap**: sliding sync's room *list* only ever enumerates joined rooms (a pre-existing limitation, not introduced here) — an ignored user's invite can only be observed there via an explicit `room_subscriptions` entry naming the room, which is where the suppression is proven. |
| `GET /_matrix/client/v3/thirdparty/{protocols,protocol/{p},location,location/{p},user,user/{p}}` | `spec-covered` | All six third-party lookup routes are wired (`src/appservice/appservice_client.cpp`'s `query_thirdparty_*` methods, routed in `client_server.cpp`). `protocols`/`protocol/{protocol}` call the owning appservice's `GET /_matrix/app/v1/thirdparty/protocol/{protocol}` and mint `instance_id` per spec; `location`/`user` (bare) fan out to every registered appservice and aggregate; `location/{protocol}`/`user/{protocol}` fan out to every appservice declaring that protocol, forwarding the client's query string verbatim as search fields. An unknown protocol is `404 M_NOT_FOUND`; `protocols` still returns `{}` (not 404) when no appservice is registered. An unreachable/erroring/timing-out appservice degrades to "no results" for the routes it would have contributed to rather than failing the whole request — proven in `tests/integration/test_appservice_thirdparty_flow.cpp`. All six routes are unauthenticated-rejected (401) and bypass client rate limiting per the spec's `Rate-limited: No` flag. Every field taken from an appservice's reply is type-checked and bounded before being echoed to the client (`appservice_client.cpp`'s `parse_thirdparty_*` — a bridge is not a trusted peer). Conformance-covered in `tests/conformance/test_appservice_thirdparty_conformance.cpp`. |
| `PUT /_matrix/client/v3/profile/{userId}/avatar_url` | `spec-covered` | Conformance fixture now covers successful update, cross-user 403 guard, and retrieval via `GET /_matrix/client/v3/profile/{userId}/avatar_url`. |
| `GET /_matrix/client/v1/rooms/{roomId}/hierarchy` | `spec-covered` | Paginated depth-first space tree with `max_depth`, `suggested_only`, and `limit`. |
| `GET /_matrix/client/v1/rooms/{roomId}/relations/{eventId}` | `spec-covered` | Direct relation scan with `dir`, `from`, `to`, `limit`, and `recurse` query parameters; supports encrypted poll responses via `m.reference`. |
| `GET /_matrix/client/v1/rooms/{roomId}/relations/{eventId}/{relType}` | `spec-covered` | Filtered by `rel_type`. |
| `GET /_matrix/client/v1/rooms/{roomId}/relations/{eventId}/{relType}/{eventType}` | `spec-covered` | Filtered by `rel_type` and child `event_type`. |
| `POST /_matrix/client/v3/rooms/{roomId}/report/{eventId}` | `spec-covered` | Accepts a reason, returns 200 `{}`, rejects malformed bodies with `M_BAD_JSON`, and surfaces reports on `GET /_matrix/client/v3/admin/safety/reports`. |
| `POST /_matrix/client/v3/rooms/{roomId}/read_markers` | `spec-covered` | Accepts `m.read`, `m.read.private`, and `m.fully_read` markers; rejects non-members with 403 `M_FORBIDDEN` and malformed bodies with 400 `M_BAD_JSON`. The `m.read` marker is reflected as an `m.receipt` ephemeral event in the next `/sync`. |
| `PUT /_matrix/client/v3/presence/{userId}/status` | `spec-covered` | Accepts `presence` and optional `status_msg`; rejects cross-user updates with 403 `M_FORBIDDEN` and malformed bodies with 400 `M_BAD_JSON`. |
| `POST /_matrix/client/v3/rooms/{roomId}/receipt/{receiptType}/{eventId}` | `spec-covered` | Accepts `m.read`, `m.read.private`, and `m.fully_read`; rejects unknown receipt types with 400 `M_INVALID_PARAM`, non-members with 403 `M_FORBIDDEN`, and malformed bodies with 400 `M_BAD_JSON`. The `m.read` receipt is reflected as an `m.receipt` ephemeral event in `/sync`. |

### Federation API

| Endpoint | Status | Needs |
| --- | --- | --- |
| `PUT /_matrix/federation/v1/send/{txnId}` inbound | `spec-covered` | Richer EDU side-effects. Idempotency, unknown-EDU discard, oversize rejection, and PDU content-hash verification now conformance-covered. |
| Server ACL enforcement (`m.room.server_acl`, MSC4436) | `spec-covered` | Per-room ACLs applied to protected federation endpoints and per-PDU/room-local EDU in inbound `/send`. Case-insensitive glob matching, IP-literal handling, and port stripping. |
| `PUT /_matrix/federation/v1/send/{txnId}` outbound | `spec-covered` | Live signed-transaction interop test against a real Synapse peer landed in 0.8.6 (opt-in `build_live_tests`). |
| Federation join/leave/invite/knock/backfill | `spec-covered` | Richer production leave/knock state semantics (stripped state, knock acceptance flows). |
| Server discovery | `spec-covered` | TLS-bound origin validation helper and conformance fixtures added; live network conformance coverage remains optional. |
| Request and event signing/verification | `spec-covered` | Live signed-request interop test against a real Synapse peer landed in 0.8.6 (opt-in). Inbound PDU content-hash verification wired and conformance-covered. |
| `GET /_matrix/federation/v1/query/profile` | `spec-covered` | |
| `GET /_matrix/federation/v1/query/directory` | `spec-covered` | |
| `GET /_matrix/federation/v1/hierarchy/{roomId}` | `spec-covered` | Space summary for remote servers. Conformance coverage added for 200, 404, invalid `suggested_only`, and missing-provider 501. |
| Event-graph queries | `spec-covered` | `auth_chain`/`auth_chain_ids` transitive-closure reconstruction landed in 0.8.9; historical state-at-event reconstruction for `/state` and `/state_ids` landed in 0.8.10 with conformance fixtures; `resolve_state_v2` with full conflicted/unconflicted partitioning is implemented and conformance-covered. |
| Outbound federation queues | `spec-covered` | Live federation delivery coverage under realistic load. |
| Key publication (`GET /_matrix/key/v2/server`) | `spec-covered` | Key-ID format, valid_until_ts expiry, old_verify_keys structural contract, and key-rotation publication (new key active, retired key in old_verify_keys) now covered by conformance fixtures. Multiple simultaneously-active signing keys are published in `verify_keys` and signed by the preferred key, with unit and conformance coverage. |
| `GET /_matrix/federation/v1/media/download/{mediaId}` inbound (serving our media to remote servers) | `spec-covered` | Authenticated by `X-Matrix` request signatures and served from the main process media repository as a `multipart/mixed` response per Matrix v1.19. Conformance coverage added for 200, 404, 451, missing-provider 501, and percent-decoded `mediaId`. The federation worker is bypassed because it has no access to the local media store. |
| `GET /_matrix/federation/v1/media/download/{mediaId}` outbound (fetching remote media) | `spec-covered` | Tries the authenticated endpoint first, falls back to the deprecated v3 endpoint on 404, and follows `Location` redirects via an SSRF-safe resolver with pinned addresses (v0.11.5). |
| `GET /_matrix/federation/v1/openid/userinfo` | `spec-covered` | Redeems a token minted by the client-server `POST /user/{userId}/openid/request_token` (see the Client-server table), returning `{"sub": "<user_id>"}`. Per spec this endpoint requires no authentication and is not rate-limited, so it is dispatched entirely outside the X-Matrix signed-request path — alongside `GET /_matrix/key/v2/server` — rather than through `federation::handle_inbound_federation_request`. An unknown or expired token gets the identical `401 M_UNKNOWN_TOKEN` response so a caller cannot distinguish the two. Conformance-covered, including that an ordinary client-server access token is rejected here. |

### Application Service API

Unimplemented in full. Tracked here so the absence is visible rather than inferred
from silence. See [spec](../matrix-v1.19-spec/application-service-api.md).

| Surface | Status | Needs |
| --- | --- | --- |
| Appservice registration files (`as_token`, `hs_token`, namespaces) | `spec-covered` | Landed 0.12.1: config parsing (`appservice.registration_files`, JSON), constant-time token storage (`core::SecretBuffer`), and namespace-based exclusivity for users and aliases (room-ID namespace exclusivity is parsed and matched via `any_exclusive_namespace_matches` but has no creation-time enforcement point, since Matrix room IDs are server-generated rather than client-chosen). Conformance-covered in `tests/conformance/test_appservice_conformance.cpp`. |
| `PUT /_matrix/app/v1/transactions/{txnId}` (outbound) | `runtime-wired` | Landed 0.12.1: `appservice::AppserviceClient::send_transaction` (`src/appservice/appservice_client.cpp`) PUTs one event per transaction, `Authorization: Bearer <hs_token>` only (no legacy `access_token` query param — see the client's doc comment). Wired synchronously into `room_service.cpp`'s `dispatch_appservice_delivery`, called from `send_event()`, `dispatch_membership_push_notification()` (local membership transitions), and the federation-accepted-PDU path (`deliver_federation_push_notifications`) — every interested, registered appservice (namespace match via `registration_interested_in_room_event`) is contacted on the SAME request that produced the event, unlike push delivery's async dispatch (see the function's doc comment for why). Delivery state (`next_txn_id`, `delivered_stream_ordering`, and the currently in-flight `pending_txn_id`/`pending_stream_ordering`) is durable in the new `appservice_txn_cursor` table (migration `013_appservice_txn_cursor.sql`, schema version 13) so a crash mid-delivery is retried — with the identical txn_id and event — on the next relevant event, satisfying the spec's at-least-once/no-alter-on-retry requirement. Unit-covered (pure transaction-body builder, disabled-appservice fast path) and exercised indirectly by the appservice conformance suite's registration/auth scenarios; **not yet conformance-covered against a live mock appservice HTTP server** (unlike the Push Gateway API's `test_push_delivery_flow.cpp`). **Known gaps:** one event per transaction rather than batching multiple pending events; no background/async dispatch (blocks the triggering request on the appservice's response, bounded by a fixed 10s/30s connect/total timeout); no retry/backoff timer independent of new events arriving (mirrors the Push Gateway client's own documented gap); `ephemeral` data (`m.presence`/`m.typing`/`m.receipt`, spec v1.13) is not sent even when `receive_ephemeral` is set; `POST /_matrix/app/v1/ping` and its client-server companion `POST /_matrix/client/v1/appservice/{id}/ping` are not implemented. |
| `GET /_matrix/app/v1/users/{userId}`, `/rooms/{roomAlias}` (outbound) | `scaffolded` | `appservice::AppserviceClient::query_user`/`query_room_alias` (`src/appservice/appservice_client.cpp`) exist and are unit-tested (the url:null disabled fast path), but are **not invoked anywhere** — no call site triggers a query on a local-lookup miss inside an appservice namespace (e.g. inviting an unregistered namespaced user, or joining an unregistered namespaced alias). Wiring those trigger points is still not-started. |
| `m.login.application_service` login type | `spec-covered` | Landed 0.12.1: `POST /register` (passwordless, UIA bypassed per spec) and `POST /login` both accept the type, restricted to the appservice's own namespace (`M_EXCLUSIVE` otherwise); `GET /login` advertises it. Conformance-covered. |
| Appservice-authenticated CS API (`?user_id=` masquerading) | `spec-covered` | Landed 0.12.1: `as_token` bearer auth defaults to the appservice's `sender_localpart` user; `?user_id=` masquerades as any user in the appservice's `users` namespace (403 otherwise); `?device_id=` is honoured only when it names a device already known to belong to that user (400 `M_UNKNOWN_DEVICE` otherwise). Conformance-covered. Known gap: the `ts` query-string timestamp-massaging parameter (spec `v1.3`) is not yet implemented. |
| `/_matrix/client/v3/thirdparty/*` (location, user, protocol) | `spec-covered` | Implemented — see the `GET /_matrix/client/v3/thirdparty/{protocols,protocol/{p},...}` row in the Client-server API table above. `protocols` returns an empty `{}` (not 404) when no appservice is registered. |

### Push Gateway API

Wired for message events and membership transitions, gated behind
`server.push.enabled` (default `false`). See
[spec](../matrix-v1.19-spec/push-gateway-api.md).

| Surface | Status | Needs |
| --- | --- | --- |
| `POST /_matrix/push/v1/notify` (outbound to gateway) | `runtime-wired` | `PushGatewayClient::notify()` posts to a pusher's gateway URL over the SSRF-safe `OutboundClient` + `CachedServerDiscovery`, parses the `rejected` pushkey list, and is driven from `room_service.cpp`'s `send_event()`, every membership-mutating path, and — since 0.11.11 — the federation `pdu_sink` callback for accepted PDUs, all off the request path. Delivery tasks are reaped once complete and capped at `k_max_in_flight_push_deliveries` (128) concurrent tasks, dropping (and logging) a delivery rather than spawning past the cap (0.11.11); each task's own work is separately capped at `k_max_pushers_per_delivery` (10) pushers per recipient per event (0.11.11). Integration-covered against a real local TLS mock gateway, including the invite path, the federation-accepted path, the drop-at-cap behaviour, and the per-recipient pusher cap. Remaining: retry/backoff (spec SHOULD) — a failed delivery attempt today is not retried. |
| Pusher persistence | `spec-covered` | `store_pusher`/`find_pusher`/`delete_pusher`/`list_pushers_for_user` back `GET /pushers`/`POST /pushers/set`; pushers survive across requests and restarts (0.11.11). |
| Push-rule evaluation | `runtime-wired` | `evaluate_push_rules()` (pure, unit- and conformance-covered since an earlier phase) is now actually invoked per sent event, and per membership transition, against every recipient's `default_push_ruleset()` (0.11.11). `.m.rule.invite_for_me` is reachable: an invitee who is not yet a joined member is added as an explicit extra recipient rather than only iterating joined members (0.11.11). |
| `counts.unread` | `runtime-wired` | Sum of `sync::count_notifications()`/`sync::read_receipt_ordering()` (the same baseline `/sync`'s `unread_notifications` block uses) across every room the recipient is joined to — no second unread implementation (0.11.11). |
| Email pushers (`kind: "email"`) | `not-started` | Accepted by body validation and persisted, but skipped at delivery time — no email transport exists. |

### Server administration

| Endpoint | Status | Needs |
| --- | --- | --- |
| `GET /_merovingian/admin/health` | `runtime-wired` | Served on the public client listener (0.11.9; previously 404 in production, only reachable via the test-only local router). Admin auth via `require_admin()` (401 missing/invalid token, 403 non-admin), rate-limited under the `/_merovingian/admin/` per-IP prefix policy (30/60s default, operator-tunable). |
| Admin media moderation | `runtime-wired` | Quarantine/release/remove use `require_admin()` with the 401/403 split (0.11.9); served and rate-limited on the client port. Remaining: operator docs. |
| Admin trust and safety review | `spec-covered` | `POST /_matrix/client/v3/admin/safety/review/{targetType}/{targetId}` is now conformance-covered for room, media, and federation_server targets, empty review bodies, invalid target types, and non-admin guards. Remaining: moderator queues and broader workflow coverage. |
| `GET/PUT /_matrix/client/v1/admin/lock/{userId}` | `runtime-wired` | Account lock admin endpoint with anti-enumeration (auth before lookup), locality, and self/other-admin guards; request-path `M_USER_LOCKED` (`soft_logout:true`) enforcement landed in 0.8.19. |
| `GET/PUT /_matrix/client/v1/admin/suspend/{userId}` | `runtime-wired` | Account suspend admin endpoint; request-path `M_USER_SUSPENDED` enforcement with the spec allowlist of permitted actions landed in 0.8.19. |
| Exported metrics | `runtime-wired` | Stable Prometheus text exposition contract documented in `docs/observability-audit.md`; `X-Merovingian-Request-Id` and `Traceparent` correlation headers landed in 0.8.11. Remaining: operator dashboards and retention/export policy. |
| Debug logging | `runtime-wired` | Per-module level filtering, wall-clock rate limits, and structured diagnostics with `request_id`/`trace_id`/`span_id` fields landed in 0.5.0/0.8.11. Remaining: formal log-format stability commitment. |
